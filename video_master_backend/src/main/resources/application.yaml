#通用配置信息
#当前激活的配置文件
spring:
  profiles:
    active: dev

#mybatis配置
mybatis:
  configuration:
    map-underscore-to-camel-case: true

  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.video_master.video_master_backend.model.entity
